import 'package:flutter/cupertino.dart';

class IngredienteScreen extends StatelessWidget{
  const IngredienteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Hola ingrediente");
  }
}